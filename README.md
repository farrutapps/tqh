# tqh

RESTful endpoints:
	POST:
	/update  # update leds on esteful board
		data structure:
		{"user_name": {id: bool}}

	GET:
	/status # get current status of leds	
		reply structure:
		{"user_name": {id: bool}}


